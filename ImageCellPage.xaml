<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms&quot;
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml&quot;
             x:Class="App1_Cell.Pagina.ImageCellPage"
             BackgroundColor="Black">
    <ContentPage.Content>
        <ListView x:Name="ListaFuncionario">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding Cargo}" Detail="{Binding Nome}"
                        ImageSource="{Binding Foto}" TextColor="CornflowerBlue" DetailColor="Red" />                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
